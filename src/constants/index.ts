import api from '../config'

const AUTH_URL = api.api.authUrl
const BASE_URL = api.api.baseUrl
const CLIENT_ID = api.api.clientId
const CLIENT_SECRET = api.api.clientSecret

export { AUTH_URL, BASE_URL, CLIENT_ID, CLIENT_SECRET }
